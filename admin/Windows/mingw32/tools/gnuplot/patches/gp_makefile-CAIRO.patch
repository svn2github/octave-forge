enable support for CAIRO terminals without wxt term

diff -r 82d535640951 config/makefile.mgw
--- a/config/makefile.mgw	Tue Jan 27 10:17:14 2009 +0100
+++ b/config/makefile.mgw	Tue Jan 27 10:18:04 2009 +0100
@@ -54,6 +54,7 @@
 NEWGD=1
 JPEG=1
 FREETYPE=1
+CAIRO=1
 
 # PDF device driver
 # Requires PNG and Z libraries based on particular PDF library used, and
@@ -240,6 +241,16 @@
 endif
 endif
 
+ifdef CAIRO
+	CFLAGS += -DHAVE_CAIROPDF
+	CAIRO_CFLAGS = $(shell pkg-config --cflags cairo)
+	CAIRO_LIBS = $(shell pkg-config --libs cairo)
+	PANGO_CFLAGS = $(shell pkg-config --cflags pangocairo)
+	PANGO_LIBS = $(shell pkg-config --libs pangocairo)
+	WX_OBJS =  gp_cairo.o
+	TERMFLAGS += $(CAIRO_CFLAGS) $(PANGO_CFLAGS)
+endif
+
 ifdef WXT
 	CFLAGS += -DWXWIDGETS
 	CXXFLAGS += $(shell wx-config --cxxflags)
