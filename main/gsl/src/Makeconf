
## Makeconf is automatically generated from Makeconf.base and Makeconf.add
## in the various subdirectories.  To regenerate, use ./autogen.sh to
## create a new ./Makeconf.in, then use ./configure to generate a new
## Makeconf.

OCTAVE_FORGE = 1

SHELL = /bin/bash

canonical_host_type = i686-pc-linux-gnu
prefix = /usr/local
exec_prefix = ${prefix}
bindir = ${exec_prefix}/bin
mandir = ${datarootdir}/man
libdir = ${exec_prefix}/lib
datadir = ${datarootdir}
infodir = ${datarootdir}/info
includedir = ${prefix}/include
datarootdir = ${prefix}/share
INSTALL = @INSTALL@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_SCRIPT = @INSTALL_SCRIPT@
INSTALL_DATA = @INSTALL_DATA@
INSTALLOCT=octinst.sh

DESTDIR =

RANLIB = ranlib
STRIP = strip
LN_S = ln -s

AWK = @AWK@

# Most octave programs will be compiled with $(MKOCTFILE).  Those which
# cannot use mkoctfile directly can request the flags that mkoctfile 
# would use as follows:
#    FLAG = $(shell $(MKOCTFILE) -p FLAG)
# The following flags are for compiling programs that are independent
# of Octave.  How confusing.
CC = gcc
CFLAGS = -g -O2
CPPFLAGS = 
CPICFLAG = -fPIC
CXX = g++
CXXFLAGS = -g -O2
CXXPICFLAG = -fPIC
F77 = gfortran
FFLAGS = -O -mieee-fp
FPICFLAG = -fPIC

OCTAVE = octave
OCTAVE_VERSION = 3.0.1
MKOCTFILE = mkoctfile -DHAVE_OCTAVE_$(ver) -v
SHLEXT = so

ver = 30
MPATH = /home/sh/Programmer/share/octave/3.0.1/site/m/octave-forge
OPATH = /home/sh/Programmer/libexec/octave/3.0.1/site/oct/i686-pc-linux-gnu/octave-forge
XPATH = /home/sh/Programmer/libexec/octave/3.0.1/site/exec/i686-pc-linux-gnu
ALTMPATH = /home/sh/Programmer/share/octave/3.0.1/site/octave-forge-alternatives/m
ALTOPATH = /home/sh/Programmer/libexec/octave/3.0.1/site/octave-forge-alternatives/oct/i686-pc-linux-gnu

HAVE_GSL=yes
GSL_MISSING=

%.o: %.c ; $(MKOCTFILE) -c $<
%.o: %.f ; $(MKOCTFILE) -c $<
%.o: %.cc ; $(MKOCTFILE) -c $<
%.oct: %.cc ; $(MKOCTFILE) $<
