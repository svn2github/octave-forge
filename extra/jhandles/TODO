- implement barseries in m-code + cleanup code
  (BarseriesObject, BaseLineObject)

- normal computation at surface boundaries

- fix re-entrant axes property setting (Position) when
  colorbar/legend object is located outside

- implement "factory" property values + support
  for special values: "default" and "factory"

- implement "perspective" projections

- support single-color specification for patch objects
